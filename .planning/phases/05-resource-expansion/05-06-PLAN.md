---
phase: 05-resource-expansion
plan: 06
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
  - "05-04"
  - "05-05"
files_modified:
  - src/client.ts
  - src/index.ts
  - tests/client.test.ts
  - tests/index.test.ts
autonomous: true

must_haves:
  truths:
    - "User can access client.teams with TeamsResource methods"
    - "User can access client.workflows with WorkflowsResource methods"
    - "User can access client.sessions with SessionsResource methods"
    - "User can access client.memories with MemoriesResource methods"
    - "User can access client.traces with TracesResource methods"
    - "User can access client.metrics with MetricsResource methods"
    - "All resource types are exported from index.ts"
  artifacts:
    - path: "src/client.ts"
      provides: "AgentOSClient with 6 new resource properties"
      contains: "readonly teams: TeamsResource"
    - path: "src/index.ts"
      provides: "Public API exports for all resources"
      exports: ["TeamsResource", "WorkflowsResource", "SessionsResource", "MemoriesResource", "TracesResource", "MetricsResource"]
  key_links:
    - from: "src/client.ts"
      to: "src/resources/*.ts"
      via: "imports and instantiates all resource classes"
      pattern: "new (Teams|Workflows|Sessions|Memories|Traces|Metrics)Resource"
---

<objective>
Integrate all new resources into AgentOSClient and export from public API.

Purpose: Make all resources accessible via client.{resource} pattern and ensure types are publicly available.
Output: Complete client with all 7 resource namespaces and full public API exports.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Files to modify
@src/client.ts
@src/index.ts

# Prior plan summaries (resources created)
@.planning/phases/05-resource-expansion/05-01-SUMMARY.md
@.planning/phases/05-resource-expansion/05-02-SUMMARY.md
@.planning/phases/05-resource-expansion/05-03-SUMMARY.md
@.planning/phases/05-resource-expansion/05-04-SUMMARY.md
@.planning/phases/05-resource-expansion/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resource namespaces to AgentOSClient</name>
  <files>src/client.ts</files>
  <action>
Update AgentOSClient to include all 6 new resource namespaces:

1. Add imports for new resource classes:
```typescript
import { TeamsResource } from "./resources/teams";
import { WorkflowsResource } from "./resources/workflows";
import { SessionsResource } from "./resources/sessions";
import { MemoriesResource } from "./resources/memories";
import { TracesResource } from "./resources/traces";
import { MetricsResource } from "./resources/metrics";
```

2. Add readonly properties to class (after existing `readonly agents`):
```typescript
readonly teams: TeamsResource;
readonly workflows: WorkflowsResource;
readonly sessions: SessionsResource;
readonly memories: MemoriesResource;
readonly traces: TracesResource;
readonly metrics: MetricsResource;
```

3. Initialize in constructor (after `this.agents = new AgentsResource(this);`):
```typescript
this.teams = new TeamsResource(this);
this.workflows = new WorkflowsResource(this);
this.sessions = new SessionsResource(this);
this.memories = new MemoriesResource(this);
this.traces = new TracesResource(this);
this.metrics = new MetricsResource(this);
```

All 7 resource namespaces initialized in constructor passing `this` (client instance).
  </action>
  <verify>
TypeScript compiles: `npm run build`
  </verify>
  <done>
AgentOSClient has 7 resource properties: agents, teams, workflows, sessions, memories, traces, metrics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Export all resource types from index.ts</name>
  <files>src/index.ts</files>
  <action>
Update index.ts to export all resource classes and option types:

1. Add exports for TeamsResource:
```typescript
export { TeamsResource } from "./resources/teams";
export type {
  TeamRunOptions,
  TeamStreamRunOptions,
  TeamContinueOptions,
} from "./resources/teams";
```

2. Add exports for WorkflowsResource:
```typescript
export { WorkflowsResource } from "./resources/workflows";
export type {
  WorkflowRunOptions,
  WorkflowStreamRunOptions,
  WorkflowContinueOptions,
} from "./resources/workflows";
```

3. Add exports for SessionsResource:
```typescript
export { SessionsResource } from "./resources/sessions";
export type {
  ListSessionsOptions,
  CreateSessionOptions,
} from "./resources/sessions";
```

4. Add exports for MemoriesResource:
```typescript
export { MemoriesResource } from "./resources/memories";
export type {
  ListMemoriesOptions,
  CreateMemoryOptions,
  UpdateMemoryOptions,
} from "./resources/memories";
```

5. Add exports for TracesResource:
```typescript
export { TracesResource } from "./resources/traces";
export type { ListTracesOptions } from "./resources/traces";
```

6. Add exports for MetricsResource:
```typescript
export { MetricsResource } from "./resources/metrics";
export type { GetMetricsOptions } from "./resources/metrics";
```

Organize exports in logical groups (keep existing structure, add new sections).
  </action>
  <verify>
TypeScript compiles and exports are accessible: `npm run build`
  </verify>
  <done>
All 6 new resource classes and their option types exported from index.ts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for new resources</name>
  <files>tests/client.test.ts, tests/index.test.ts</files>
  <action>
Add tests to verify resource integration:

**tests/client.test.ts:**
Add tests in existing describe block for resource namespaces:

```typescript
describe("resource namespaces", () => {
  // ... existing agents test

  it("exposes teams resource", () => {
    expect(client.teams).toBeInstanceOf(TeamsResource);
  });

  it("exposes workflows resource", () => {
    expect(client.workflows).toBeInstanceOf(WorkflowsResource);
  });

  it("exposes sessions resource", () => {
    expect(client.sessions).toBeInstanceOf(SessionsResource);
  });

  it("exposes memories resource", () => {
    expect(client.memories).toBeInstanceOf(MemoriesResource);
  });

  it("exposes traces resource", () => {
    expect(client.traces).toBeInstanceOf(TracesResource);
  });

  it("exposes metrics resource", () => {
    expect(client.metrics).toBeInstanceOf(MetricsResource);
  });
});
```

Import all resource classes at top of file.

**tests/index.test.ts:**
Add tests for new exports:

```typescript
// Add to existing import list for testing
import {
  TeamsResource,
  WorkflowsResource,
  SessionsResource,
  MemoriesResource,
  TracesResource,
  MetricsResource,
} from "../src";

// Add in exports test section
it("exports TeamsResource", () => {
  expect(TeamsResource).toBeDefined();
});
it("exports WorkflowsResource", () => {
  expect(WorkflowsResource).toBeDefined();
});
it("exports SessionsResource", () => {
  expect(SessionsResource).toBeDefined();
});
it("exports MemoriesResource", () => {
  expect(MemoriesResource).toBeDefined();
});
it("exports TracesResource", () => {
  expect(TracesResource).toBeDefined();
});
it("exports MetricsResource", () => {
  expect(MetricsResource).toBeDefined();
});
```
  </action>
  <verify>
All tests pass: `npm test`
  </verify>
  <done>
Integration tests verify all 6 new resource namespaces are accessible and exported correctly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` - TypeScript compiles without errors
2. `npm test` - All tests pass (existing + new integration tests)
3. `npm test -- tests/client.test.ts` - Client integration tests pass
4. `npm test -- tests/index.test.ts` - Export tests pass
5. Verify all 7 resources accessible via client instance
</verification>

<success_criteria>
- AgentOSClient has 7 resource properties (agents + 6 new)
- All resource classes exported from index.ts
- All option types exported from index.ts
- Integration tests verify resource availability
- Full test suite passes (~200+ tests)
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-resource-expansion/05-06-SUMMARY.md`
</output>
