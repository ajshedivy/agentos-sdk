---
phase: 07-runtime-support-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README documents how to install the SDK"
    - "README shows usage examples for core resources"
    - "README documents streaming patterns (iterator and events)"
    - "README explains file upload handling"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive SDK documentation"
      min_lines: 200
      contains: "npm install"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "installation command"
      pattern: "@ajshedivy/agentos-sdk"
---

<objective>
Create comprehensive README documentation for the SDK

Purpose: Enable developers to quickly understand and use the SDK with clear examples
Output: Well-organized README with installation, usage examples per resource, and API reference
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-runtime-support-polish/07-CONTEXT.md
@.planning/phases/07-runtime-support-polish/07-RESEARCH.md

@package.json
@src/index.ts
@src/client.ts
@src/resources/agents.ts
@src/resources/teams.ts
@src/resources/workflows.ts
@src/resources/knowledge.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive README.md</name>
  <files>README.md</files>
  <action>
Create README.md following the Python docs structure (https://docs.agno.com/reference/clients/agentos-client):

## Structure (in order):
1. **Title and badges** - Package name, npm version badge
2. **Quick Start** - Installation and minimal example (5 lines or less)
3. **Installation** - npm/yarn/pnpm commands
4. **Usage Examples** - One focused example per resource:
   - Agents: list, run (non-streaming), runStream with iterator pattern
   - Teams: list, runStream with event emitter pattern
   - Workflows: list, run
   - Sessions: create, list, getRuns
   - Memories: list, create, search
   - Knowledge: upload (file, URL, text), search
   - Traces: list with filters
   - Metrics: get, refresh
5. **Streaming** - Show both patterns (async iterator and event emitter) with real code examples
6. **File Uploads** - Document FileInput types (Buffer, string path, Blob, File, ReadStream), show media parameters
7. **Error Handling** - Document error classes (APIError, NotFoundError, etc.)
8. **TypeScript** - Note about full TypeScript support, mention generated types from OpenAPI
9. **API Reference** - Link to generated types, brief method signatures per resource
10. **Requirements** - Node.js 18+ (note that fetch is experimental in Node 18, stable in Node 21+)
11. **License** - MIT

## Guidelines (from CONTEXT.md):
- Small, focused examples - not exhaustive but representative
- One example per resource concept
- TypeScript code examples throughout (this is a TypeScript SDK)
- Runtime limitations documented in relevant sections (e.g., ReadStream in file uploads section)
- Do NOT create a prominent "Limitations" section
- Do NOT include browser examples or browser-specific content (deferred to v2)

## Example structure for each resource section:
```typescript
// Brief description of what this shows
const client = new AgentOSClient({ apiKey: '...' });
const result = await client.resource.method({ param: value });
console.log(result);
```

## File upload example must show:
- Buffer input
- File path input (Node.js only)
- Multiple files via array

## Streaming examples must show:
- for await...of pattern (iterator)
- .on() pattern (event emitter)
- Stream cancellation
  </action>
  <verify>
Read README.md and verify:
1. All sections present
2. Examples are syntactically correct TypeScript
3. No browser-specific examples
4. Node.js 18+ requirement documented
  </verify>
  <done>
README provides complete documentation with installation, examples for all resources, streaming patterns, file uploads, and error handling
  </done>
</task>

</tasks>

<verification>
1. README.md exists with all required sections
2. Code examples are valid TypeScript syntax
3. Installation command matches package name in package.json
4. No mention of browser usage (deferred)
5. Node.js 18+ requirement clearly stated
</verification>

<success_criteria>
- README.md created with ~200+ lines of documentation
- Installation, usage, streaming, file uploads, errors all documented
- One example per resource type
- TypeScript examples throughout
- Node.js runtime requirements documented
</success_criteria>

<output>
After completion, create `.planning/phases/07-runtime-support-polish/07-02-SUMMARY.md`
</output>
