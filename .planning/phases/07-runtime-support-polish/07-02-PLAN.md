---
phase: 07-runtime-support-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - examples/browser/index.html
  - examples/browser/app.js
autonomous: true

must_haves:
  truths:
    - "Developers can understand how to install and use the SDK from README"
    - "Browser usage example exists and is testable"
    - "Node.js 18 experimental fetch status is documented"
  artifacts:
    - path: "README.md"
      provides: "Package documentation with installation, usage, API reference"
      min_lines: 150
    - path: "examples/browser/index.html"
      provides: "Browser testing page"
      min_lines: 20
    - path: "examples/browser/app.js"
      provides: "Browser usage example code"
      min_lines: 30
  key_links:
    - from: "README.md"
      to: "src/index.ts"
      via: "import examples"
      pattern: "import.*agentos-sdk"
---

<objective>
Create comprehensive README documentation and browser usage example for manual testing.

Purpose: Enable developers to use the SDK with clear documentation and provide browser testing capability.
Output: README.md with full documentation, browser example for manual compatibility testing.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-runtime-support-polish/07-RESEARCH.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive README</name>
  <files>README.md</files>
  <action>
    Create README.md with the following sections:

    1. **Title and badges** (npm version placeholder, license, TypeScript)

    2. **Introduction** (2-3 sentences)
       - What the SDK is (TypeScript SDK for AgentOS API)
       - Core value proposition (run agents, teams, workflows with streaming in 5 lines)

    3. **Installation**
       ```bash
       npm install @ajshedivy/agentos-sdk
       ```

    4. **Quick Start** (show 5-line example)
       ```typescript
       import { AgentOSClient } from '@ajshedivy/agentos-sdk';

       const client = new AgentOSClient({ baseUrl: 'https://your-agentos-instance.com' });
       for await (const event of client.agents.runStream('agent-id', { message: 'Hello' })) {
         if (event.event === 'RunContentEvent') console.log(event.data.content);
       }
       ```

    5. **Features** (bullet list)
       - Full TypeScript types generated from OpenAPI
       - Dual streaming interfaces (async iterators + event emitters)
       - Support for agents, teams, workflows, sessions, memories, knowledge, traces, metrics
       - File uploads (images, audio, video, documents)
       - Cross-platform (Node.js 18+ and modern browsers)

    6. **Usage Examples**
       - Non-streaming agent run
       - Streaming with async iterator
       - Streaming with event emitter
       - File uploads with agent run
       - Knowledge base search
       - Session management

    7. **API Reference** (brief overview with links to types)
       - AgentOSClient constructor options
       - Resource namespaces (agents, teams, workflows, etc.)
       - Error types (APIError, BadRequestError, etc.)
       - Stream events (RunStartedEvent, RunContentEvent, etc.)

    8. **Runtime Support**
       - Node.js 18+ (note: fetch is experimental in Node 18, stable in Node 21+)
       - Modern browsers (Chrome, Safari, Firefox, Edge)
       - Note about file path inputs being Node.js only

    9. **Error Handling** (example with try/catch)

    10. **TypeScript** (mention strict types, generated from OpenAPI)

    11. **Contributing** (brief)

    12. **License** (MIT)

    Use clean markdown formatting. Include code examples with proper TypeScript syntax highlighting.
    Do NOT include emojis unless specifically requested.
  </action>
  <verify>
    README.md exists with all sections.
    Code examples use correct SDK API.
    `cat README.md | wc -l` shows 150+ lines.
  </verify>
  <done>
    README.md provides complete documentation for SDK installation, usage, and API reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create browser example for manual testing</name>
  <files>examples/browser/index.html, examples/browser/app.js</files>
  <action>
    Create examples/browser/ directory with browser testing files.

    **examples/browser/index.html:**
    - Simple HTML page with form for testing SDK
    - Input field for AgentOS base URL
    - Input field for agent ID
    - Input field for message
    - Button to run agent (non-streaming)
    - Button to run agent (streaming)
    - Output area for results
    - Include script tag loading from CDN or local bundle (use esm.sh for ESM import)
    - Style with minimal inline CSS for usability

    **examples/browser/app.js:**
    - Import SDK using dynamic import from esm.sh (for testing without build)
    - Initialize client with URL from form
    - Implement runAgent() function for non-streaming
    - Implement runAgentStream() function for streaming with event display
    - Handle errors and display in output area
    - Show events as they arrive for streaming

    Add comments explaining this is for manual browser compatibility testing.
    Include instructions at top of index.html on how to use (open in browser, CORS considerations).

    Note: This example uses esm.sh CDN for simplicity. Users building production apps would bundle the SDK.
  </action>
  <verify>
    examples/browser/index.html exists and is valid HTML.
    examples/browser/app.js exists with SDK usage.
    Files can be opened locally in browser (may need CORS proxy for actual API calls).
  </verify>
  <done>
    Browser example exists for manual compatibility testing. Can be opened in Chrome/Safari/Firefox to verify SDK loads and APIs work.
  </done>
</task>

</tasks>

<verification>
- README.md exists with 150+ lines
- Browser example files exist in examples/browser/
- Code examples in README use correct SDK imports and methods
- `npm run build` still succeeds (no regressions)
</verification>

<success_criteria>
- README provides complete documentation for SDK users
- Browser example enables manual testing without build step
- Node.js 18 experimental status documented
- All code examples are syntactically correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-runtime-support-polish/07-02-SUMMARY.md`
</output>
